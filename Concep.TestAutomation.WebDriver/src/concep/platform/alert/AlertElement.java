package concep.platform.alert;

import concep.platform.core.PlatformElement;
import concep.seleniumWD.core.SeleniumDriverCore;

public class AlertElement extends PlatformElement {

    public AlertElement( SeleniumDriverCore driver ) {
        super( driver );
    }

}
